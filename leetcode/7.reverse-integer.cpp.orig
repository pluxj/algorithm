/*
 * @lc app=leetcode id=7 lang=cpp
 *
 * [7] Reverse Integer
 */

// @lc code=start
class Solution {
public:
    int reverse(int x) {
<<<<<<< HEAD
        int flag = 1;
        if(x < 0 )
            flag = -1;
        int ret = 0;
        while(x / 10)
        {
            if(ret > INT_MAX/10 || (ret == INT_MAX / 10 && x/10 > 7))
                return 0;
            if(ret < INT_MIN / 10 || (ret == INT_MIN / 10 && x / 10 < -8))
                return 0;
            ret = ret * 10 + x%10;
            x = x / 10;
=======
      
           int rev = 0;
        while (x != 0) {
            int pop = x % 10;
            x /= 10;
            if (rev > INT_MAX/10 || (rev == INT_MAX / 10 && pop > 7)) return 0;
            if (rev < INT_MIN/10 || (rev == INT_MIN / 10 && pop < -8)) return 0;
            rev = rev * 10 + pop;
>>>>>>> 8a268830f9bcb485f8a49ea0f635241a88e1b615
        }
        return rev;
      
    }
};
// @lc code=end

